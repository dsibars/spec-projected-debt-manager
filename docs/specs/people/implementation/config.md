# People Module Implementation

- Use `@shared/skills/ui/react` for all components.
- Use `@shared/skills/persistence/localstorage` for [[models/Person]] and [[models/Group]] storage.
- Adhere to `@shared/skills/platform/static-web` constraints.
