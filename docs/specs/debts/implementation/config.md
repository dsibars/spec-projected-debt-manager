# Debts Module Implementation

- Use `@shared/skills/ui/react` for all components.
- Use `@shared/skills/persistence/localstorage` for [[models/Debt]] and [[models/Tag]] storage.
- Adhere to `@shared/skills/platform/static-web` constraints.
